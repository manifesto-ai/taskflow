# Domain Scaffold

This directory is reserved for the MEL domain source.

Planned file:

- `taskflow.mel`

Expected responsibility:

- define TaskFlow state, computed values, and actions
- remain the single semantic source of truth for the app domain
- avoid reintroducing JS-first domain logic from the deleted v1 stack
